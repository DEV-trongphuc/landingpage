(function () {
if (!document.querySelector('script[src*="googletagmanager.com/gtag/js"]')) {
var script = document.createElement('script');
script.async = true;
script.src = 'https://www.googletagmanager.com/gtag/js?id=G-QKV7LKNLLH';
document.head.appendChild(script);
}
window.dataLayer = window.dataLayer || [];
if (!window.gtag) {
window.gtag = function () {
window.dataLayer.push(arguments);
};
window.gtag('js', new Date());
window.gtag('config', 'G-QKV7LKNLLH');
window.gtag('config', 'AW-11205917800');
}
if (!window.gtag_report_conversion) {
window.gtag_report_conversion = function (url) {
var callback = function () {
if (typeof (url) != 'undefined') {
window.location = url;
}
};
window.gtag('event', 'conversion', {
'send_to': 'AW-11205917800/mdXJCOTL-bccEOj4st8p',
'value': 1.0,
'currency': 'VND',
'event_callback': callback
});
return false;
};
}
})();
const isEn = document.documentElement.lang === 'en';
(function () {
'use strict';
const isEn = document.documentElement.lang === 'en';
function lockScroll() {
document.body.style.overflow = 'hidden';
if (window.lenis) {
window.lenis.stop();
}
}
function unlockScroll() {
document.body.style.overflow = '';
if (window.lenis) {
window.lenis.start();
}
}
window.lockScroll = lockScroll;
window.unlockScroll = unlockScroll;
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
anchor.addEventListener('click', function (e) {
if (typeof closeMobileMenu === 'function') {
closeMobileMenu();
}
const targetId = this.getAttribute('href');
if (targetId === '#' || targetId === '#dang-ky') return;
const target = document.querySelector(targetId);
if (!target) return;
e.preventDefault();
const headerH = document.getElementById('site-header')?.offsetHeight || 68;
const top = target.getBoundingClientRect().top + window.scrollY - headerH - 16;
window.scrollTo({ top, behavior: 'smooth' });
});
});
const header = document.getElementById('site-header');
const floatingCta = document.getElementById('floating-cta');
const heroCta = document.getElementById('hero-main-cta');
let lastScrollTop = 0;
function onScroll() {
const scrollY = window.scrollY;
if (header) {
header.classList.toggle('scrolled', scrollY > 40);
if (scrollY > 100 && scrollY > lastScrollTop) {
header.classList.add('hide');
} else if (scrollY < lastScrollTop) {
header.classList.remove('hide');
}
}
lastScrollTop = scrollY;
if (floatingCta && heroCta) {
const heroBottom = heroCta.getBoundingClientRect().bottom;
floatingCta.classList.toggle('show', heroBottom < 0);
floatingCta.setAttribute('aria-hidden', heroBottom >= 0 ? 'true' : 'false');
const floatingBtn = floatingCta.querySelector('.floating-btn');
if (floatingBtn) {
floatingBtn.setAttribute('tabindex', heroBottom < 0 ? '0' : '-1');
}
}
}
let isScrollThrottled = false;
window.addEventListener('scroll', () => {
if (!isScrollThrottled) {
isScrollThrottled = true;
requestAnimationFrame(() => {
onScroll();
isScrollThrottled = false;
});
}
}, { passive: true });
onScroll();
const hamburger = document.getElementById('hamburger');
const mobileMenu = document.getElementById('mobile-menu');
const mobileOverlay = document.getElementById('mobile-overlay');
const mobileMenuClose = document.getElementById('mobile-menu-close');
function closeMobileMenu() {
if (mobileMenu) {
mobileMenu.classList.remove('open');
mobileMenu.setAttribute('aria-hidden', 'true');
}
if (mobileOverlay) {
mobileOverlay.classList.remove('visible');
}
if (hamburger) {
hamburger.classList.remove('active');
hamburger.setAttribute('aria-expanded', 'false');
}
}
if (hamburger && mobileMenu) {
hamburger.addEventListener('click', () => {
const isOpen = mobileMenu.classList.contains('open');
if (isOpen) {
closeMobileMenu();
} else {
mobileMenu.classList.add('open');
mobileMenu.setAttribute('aria-hidden', 'false');
hamburger.classList.add('active');
hamburger.setAttribute('aria-expanded', 'true');
if (mobileOverlay) mobileOverlay.classList.add('visible');
}
});
if (mobileOverlay) {
mobileOverlay.addEventListener('click', closeMobileMenu);
}
if (mobileMenuClose) {
mobileMenuClose.addEventListener('click', closeMobileMenu);
}
document.addEventListener('click', (e) => {
if (hamburger && mobileMenu && !hamburger.contains(e.target) && !mobileMenu.contains(e.target)) {
closeMobileMenu();
}
});
}
const revealEls = document.querySelectorAll('.reveal-up');
if ('IntersectionObserver' in window) {
const observer = new IntersectionObserver(
(entries) => {
entries.forEach((entry, i) => {
if (entry.isIntersecting) {
const siblings = entry.target.parentElement
? [...entry.target.parentElement.querySelectorAll('.reveal-up')]
: [entry.target];
const idx = siblings.indexOf(entry.target);
const delay = Math.min(idx * 80, 400);
setTimeout(() => {
entry.target.classList.add('visible');
}, delay);
observer.unobserve(entry.target);
}
});
},
{ threshold: 0.12, rootMargin: '0px 0px -40px 0px' }
);
revealEls.forEach(el => observer.observe(el));
} else {
revealEls.forEach(el => el.classList.add('visible'));
}
const testimonialCards = document.querySelectorAll('.testimonial-card');
const dots = document.querySelectorAll('.dot');
let currentTesti = 0;
let testiInterval = null;
function showTestimonial(idx) {
testimonialCards.forEach((card, i) => {
card.classList.toggle('active', i === idx);
});
dots.forEach((dot, i) => {
dot.classList.toggle('active', i === idx);
dot.setAttribute('aria-selected', i === idx ? 'true' : 'false');
});
currentTesti = idx;
}
dots.forEach((dot, idx) => {
dot.addEventListener('click', () => {
clearInterval(testiInterval);
showTestimonial(idx);
startTestiAutoPlay();
});
});
function startTestiAutoPlay() {
testiInterval = setInterval(() => {
const next = (currentTesti + 1) % testimonialCards.length;
showTestimonial(next);
}, 5000);
}
if (testimonialCards.length > 0) {
startTestiAutoPlay();
}
const painCards = document.querySelectorAll('.pain-card');
if ('IntersectionObserver' in window) {
const painObserver = new IntersectionObserver(
(entries) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
const idx = parseInt(entry.target.dataset.index || '0', 10);
setTimeout(() => {
entry.target.style.opacity = '1';
entry.target.style.transform = 'translateY(0)';
}, idx * 80);
painObserver.unobserve(entry.target);
}
});
},
{ threshold: 0.1 }
);
painCards.forEach(card => {
card.style.opacity = '0';
card.style.transform = 'translateY(24px)';
card.style.transition = 'opacity 0.5s ease, transform 0.5s ease, background 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease';
painObserver.observe(card);
});
}
function animateCounter(el, target, suffix, duration = 1200) {
const start = performance.now();
const isDecimal = target % 1 !== 0;
function update(time) {
const elapsed = time - start;
const progress = Math.min(elapsed / duration, 1);
const eased = 1 - Math.pow(1 - progress, 3);
const current = eased * target;
el.textContent = (isDecimal ? current.toFixed(1) : Math.floor(current)) + suffix;
if (progress < 1) requestAnimationFrame(update);
}
requestAnimationFrame(update);
}
const solStats = document.querySelectorAll('.sol-stat');
if ('IntersectionObserver' in window && solStats.length > 0) {
const statsObserver = new IntersectionObserver(
(entries) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
const valueEl = entry.target.querySelector('.sol-stat-value');
if (valueEl && !valueEl.dataset.animated) {
valueEl.dataset.animated = 'true';
const text = valueEl.textContent;
if (text.includes('100')) animateCounter(valueEl, 100, '%');
else if (text.includes('16')) animateCounter(valueEl, 18, '', 1000);
else if (text.includes('12')) {
setTimeout(() => { valueEl.textContent = '12 + 1'; }, 1200);
animateCounter(valueEl, 12, '', 1100);
}
else if (text.includes('90')) animateCounter(valueEl, 90, '');
}
statsObserver.unobserve(entry.target);
}
});
},
{ threshold: 0.5 }
);
solStats.forEach(s => statsObserver.observe(s));
}
const audienceCard = document.querySelector('.audience-card');
if (audienceCard && 'IntersectionObserver' in window) {
const audObserver = new IntersectionObserver(
(entries) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
entry.target.classList.add('in-view');
audObserver.unobserve(entry.target);
}
});
},
{ threshold: 0.3 }
);
audObserver.observe(audienceCard);
} else if (audienceCard) {
audienceCard.classList.add('in-view');
}
function initForm(formId, successId) {
const form = document.getElementById(formId);
const formSuccess = document.getElementById(successId);
if (!form) return;
const submitBtn = form.querySelector('button[type="submit"]');
const btnText = submitBtn.querySelector('span') || submitBtn;
const fields = {
fullname: {
el: form.querySelector('[name="fullname"]'),
errorEl: form.querySelector('.form-error[id*="fullname-error"]')
},
phone: {
el: form.querySelector('[name="phone"]'),
errorEl: form.querySelector('.form-error[id*="phone-error"]')
},
email: {
el: form.querySelector('[name="email"]'),
errorEl: form.querySelector('.form-error[id*="email-error"]')
},
education: {
el: form.querySelector('[name="education"]'),
errorEl: form.querySelector('.form-error[id*="education-error"]')
},
english: {
el: form.querySelector('[name="english"]'),
errorEl: form.querySelector('.form-error[id*="english-error"]')
},
message: {
el: form.querySelector('[name="message"]'),
errorEl: form.querySelector('.form-error[id*="message-error"]')
},
};
function validatePhone(val) {
return /^(0|\+84)[0-9]{8,10}$/.test(val.replace(/\s/g, ''));
}
function validateEmail(val) {
return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(val.trim());
}
function showError(field, msg) {
if (!field.el || !field.errorEl) return;
field.el.classList.add('error');
field.errorEl.textContent = msg;
}
function clearError(field) {
if (!field.el || !field.errorEl) return;
field.el.classList.remove('error');
field.errorEl.textContent = '';
}
if (fields.fullname.el) {
fields.fullname.el.addEventListener('blur', () => {
const val = fields.fullname.el.value.trim();
if (!val) showError(fields.fullname, isEn ? 'Please enter your full name.' : 'Vui lòng nhập họ và tên.');
else if (val.length < 2) showError(fields.fullname, isEn ? 'Full name must be at least 2 characters.' : 'Họ tên phải có ít nhất 2 ký tự.');
else clearError(fields.fullname);
});
}
if (fields.phone.el) {
fields.phone.el.addEventListener('blur', () => {
const val = fields.phone.el.value.trim();
if (!val) showError(fields.phone, isEn ? 'Please enter your phone number.' : 'Vui lòng nhập số điện thoại.');
else if (!validatePhone(val)) showError(fields.phone, isEn ? 'Invalid phone number.' : 'Số điện thoại không hợp lệ.');
else clearError(fields.phone);
});
}
if (fields.email.el) {
fields.email.el.addEventListener('blur', () => {
const val = fields.email.el.value.trim();
if (!val) showError(fields.email, isEn ? 'Please enter your email.' : 'Vui lòng nhập email.');
else if (!validateEmail(val)) showError(fields.email, isEn ? 'Invalid email address.' : 'Địa chỉ email không hợp lệ.');
else clearError(fields.email);
});
}
if (fields.education.el) {
fields.education.el.addEventListener('change', () => {
if (!fields.education.el.value) showError(fields.education, isEn ? 'Please select your education level.' : 'Vui lòng chọn trình độ học vấn.');
else clearError(fields.education);
});
}
if (fields.english.el) {
fields.english.el.addEventListener('change', () => {
if (!fields.english.el.value) showError(fields.english, isEn ? 'Please select your English level.' : 'Vui lòng chọn trình độ Tiếng Anh.');
else clearError(fields.english);
});
}
Object.values(fields).forEach(f => {
if (f.el) f.el.addEventListener('input', () => clearError(f));
});
form.addEventListener('submit', function (e) {
e.preventDefault();
let valid = true;
const nameVal = fields.fullname.el ? fields.fullname.el.value.trim() : '';
if (!nameVal || nameVal.length < 2) {
showError(fields.fullname, isEn ? 'Please enter a valid full name.' : 'Vui lòng nhập họ và tên hợp lệ.');
valid = false;
} else clearError(fields.fullname);
const phoneVal = fields.phone.el ? fields.phone.el.value.trim() : '';
if (!phoneVal || !validatePhone(phoneVal)) {
showError(fields.phone, isEn ? 'Please enter a valid phone number.' : 'Vui lòng nhập số điện thoại hợp lệ.');
valid = false;
} else clearError(fields.phone);
const emailVal = fields.email.el ? fields.email.el.value.trim() : '';
if (!emailVal || !validateEmail(emailVal)) {
showError(fields.email, isEn ? 'Please enter a valid email address.' : 'Vui lòng nhập địa chỉ email hợp lệ.');
valid = false;
} else clearError(fields.email);
const eduVal = fields.education.el ? fields.education.el.value : '';
if (!eduVal) {
showError(fields.education, isEn ? 'Please select your education level.' : 'Vui lòng chọn trình độ học vấn.');
valid = false;
} else clearError(fields.education);
const engVal = fields.english.el ? fields.english.el.value : '';
if (!engVal) {
showError(fields.english, isEn ? 'Please select your English level.' : 'Vui lòng chọn trình độ Tiếng Anh.');
valid = false;
} else clearError(fields.english);
if (!valid) {
submitBtn.style.animation = 'shake 0.4s ease';
setTimeout(() => { submitBtn.style.animation = ''; }, 400);
return;
}
submitBtn.disabled = true;
const originalText = btnText.textContent;
btnText.textContent = isEn ? 'Sending...' : 'Đang gửi...';
submitBtn.style.opacity = '0.7';
const eduEl = fields.education ? fields.education.el : null;
const engEl = fields.english ? fields.english.el : null;
const msgEl = fields.message ? fields.message.el : null;
const msgVal = msgEl ? msgEl.value.trim() : '';
const getSelectText = (el) => {
if (!el) return '';
const val = el.value;
if (!val) return '';
if (el.selectedIndex >= 0) return el.options[el.selectedIndex].text;
const mapping = isEn ? {
'highschool': 'High School',
'college': 'College/Diploma',
'bachelor': 'Bachelor',
'master': 'Master',
'other': 'Other',
'below-5.0': 'Below IELTS 5.0',
'5.0-5.5': 'IELTS 5.0 - 5.5',
'6.0-plus': 'IELTS 6.0+'
} : {
'highschool': 'THPT',
'college': 'Cao đẳng',
'bachelor': 'Cử nhân',
'master': 'Thạc sĩ',
'other': 'Khác',
'below-5.0': 'Dưới IELTS 5.0',
'5.0-5.5': 'IELTS 5.0 - 5.5',
'6.0-plus': 'IELTS 6.0+'
};
return mapping[val] || val;
};
const eduText = getSelectText(eduEl);
const engText = getSelectText(engEl);
const noteParts = [];
if (eduText) noteParts.push((isEn ? 'Education: ' : 'Học vấn: ') + eduText);
if (engText) noteParts.push((isEn ? 'English: ' : 'Tiếng Anh: ') + engText);
if (msgVal) noteParts.push(msgVal);
const ctaSource = (formId === 'modal-cta-form') ? activeCtaSource : 'inline_form';
noteParts.push('CTA Source: ' + ctaSource);
const combinedNote = noteParts.join(' | ');
let sourceVal = "Landing_MBA";
let chuongTrinhVal = "Online MBA";
let resolvedProgramKey = null;
if (formId === 'modal-cta-form' && activeCtaSource) {
if (activeCtaSource.startsWith('pathfinder-')) {
resolvedProgramKey = activeCtaSource.replace('pathfinder-', '');
} else if (['IDEAS01', 'IDEAS02', 'IDEAS03', 'IDEAS04', 'IDEAS05', 'IDEAS06', 'IDEAS07'].includes(activeCtaSource)) {
resolvedProgramKey = activeCtaSource;
}
}
if (!resolvedProgramKey) {
const programSelect = form.querySelector('[name="program"]');
const selectedProgramVal = programSelect ? programSelect.value : '';
if (selectedProgramVal) {
if (selectedProgramVal === 'Full BBA') resolvedProgramKey = 'IDEAS01';
else if (selectedProgramVal === 'Top-up BBA') resolvedProgramKey = 'IDEAS07';
else if (selectedProgramVal === 'Online MBA') resolvedProgramKey = 'IDEAS02';
else if (selectedProgramVal === 'Executive MBA') resolvedProgramKey = 'IDEAS03';
else if (selectedProgramVal === 'MBA in AI') resolvedProgramKey = 'IDEAS05';
else if (selectedProgramVal === 'MSc AI') resolvedProgramKey = 'IDEAS04';
else if (selectedProgramVal === 'Dual DBA') resolvedProgramKey = 'IDEAS06';
else if (selectedProgramVal === 'Lộ trình phù hợp' || selectedProgramVal === 'Suitable Roadmap') {
resolvedProgramKey = 'ROADMAP';
}
} else {
const path = window.location.pathname.toLowerCase();
if (path.includes("mbainai")) {
resolvedProgramKey = 'IDEAS05';
} else if (path.includes("mscinai") || path.includes("mscai")) {
resolvedProgramKey = 'IDEAS04';
} else if (path.includes("emba")) {
resolvedProgramKey = 'IDEAS03';
} else if (path.includes("fullbba")) {
resolvedProgramKey = 'IDEAS01';
} else if (path.includes("bba")) {
resolvedProgramKey = 'IDEAS07';
} else if (path.includes("dual-dba") || path.includes("dba")) {
resolvedProgramKey = 'IDEAS06';
} else if (/(?:^|\/)mba(?:\.html|\/|$)/.test(path)) {
resolvedProgramKey = 'IDEAS02';
}
}
}
if (resolvedProgramKey === 'IDEAS05') {
sourceVal = "Landing_MBA_AI";
chuongTrinhVal = "Online MBA in AI";
} else if (resolvedProgramKey === 'IDEAS04') {
sourceVal = "Landing_MSc_AI";
chuongTrinhVal = "Online MSc AI";
} else if (resolvedProgramKey === 'IDEAS03') {
sourceVal = "Landing_EMBA";
chuongTrinhVal = "Online EMBA";
} else if (resolvedProgramKey === 'IDEAS01') {
sourceVal = "Landing_BBA_Full";
chuongTrinhVal = "Online Full BBA";
} else if (resolvedProgramKey === 'IDEAS07') {
sourceVal = "Landing_BBA_Topup";
chuongTrinhVal = "Online Top-up BBA";
} else if (resolvedProgramKey === 'IDEAS06') {
sourceVal = "Landing_Dual_DBA";
chuongTrinhVal = "Online Dual DBA";
} else if (resolvedProgramKey === 'IDEAS02') {
sourceVal = "Landing_MBA";
chuongTrinhVal = "Online MBA";
} else if (resolvedProgramKey === 'ROADMAP') {
sourceVal = isEn ? "Landing_Roadmap" : "Landing_Lo_Trinh";
chuongTrinhVal = isEn ? "Suitable Roadmap" : "Lộ trình phù hợp";
} else {
sourceVal = "Landing_General";
chuongTrinhVal = "General International Program";
}
const payload = {
form_id: "3ebd9ef0f28f4d1bd0a4d56b089e1c25",
email: emailVal,
firstName: nameVal,
phoneNumber: phoneVal,
hoc_van: eduText || eduVal,
tieng_anh: engText || engVal,
khach_note: combinedNote,
chuong_trinh: chuongTrinhVal
};
const webhookPayload = {
name: nameVal,
phone: phoneVal,
email: emailVal,
source: sourceVal,
type: (formId === 'modal-cta-form') ? ctaSource : "form_dang_ky",
hoc_van: eduText || eduVal,
tieng_anh: engText || engVal,
nhu_cau: (msgVal ? msgVal + " | " : "") + "Submit từ CTA: " + ctaSource,
chuong_trinh: chuongTrinhVal
};
const urlParams = new URLSearchParams(window.location.search);
const utmParams = ['utm_campaign', 'utm_source', 'utm_medium', 'utm_content', 'utm_term'];
utmParams.forEach(param => {
const val = urlParams.get(param);
if (val) {
webhookPayload[param] = val;
}
});
const p1 = fetch("https://automation.ideas.edu.vn/mail_api/forms.php?route=submit", {
method: "POST",
headers: { "Content-Type": "application/json" },
body: JSON.stringify(payload)
});
const p2 = fetch("https://open.domation.net/sale_data/webhook.php?token=tok_kjhbs32a", {
method: "POST",
headers: { "Content-Type": "application/json" },
body: JSON.stringify(webhookPayload)
});
Promise.allSettled([p1, p2])
.then(results => {
results.forEach((res, index) => {
if (res.status === 'rejected') {
console.error(`Lỗi gửi form cổng ${index + 1}:`, res.reason);
}
});
form.style.display = 'none';
if (formSuccess) formSuccess.style.display = 'block';
if (formId === 'cta-form') {
formSuccess.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
}
if (typeof window.dataLayer !== 'undefined') {
window.dataLayer.push({
event: 'form_submit',
form_id: 'mba-lead-form',
form_type: formId === 'modal-cta-form' ? 'modal' : 'inline'
});
}
if (typeof gtag_report_conversion === 'function') {
gtag_report_conversion();
}
})
.catch(err => {
console.error('Lỗi tổng thể submit form:', err);
form.style.display = 'none';
if (formSuccess) formSuccess.style.display = 'block';
})
.finally(() => {
submitBtn.disabled = false;
btnText.textContent = originalText;
submitBtn.style.opacity = '1';
});
});
}
initForm('cta-form', 'form-success');
initForm('modal-cta-form', 'modal-form-success');
const regModal = document.getElementById('reg-modal');
const regClose = document.getElementById('reg-modal-close');
const regOverlay = document.getElementById('reg-modal-overlay');
let activeCtaSource = 'unknown';
function openRegModal(ctaSource) {
if (!regModal) return;
let resolvedSource = 'general_cta';
if (ctaSource) {
if (typeof ctaSource === 'object' && ctaSource.innerText) {
resolvedSource = ctaSource.innerText.replace(/\s+/g, ' ').trim();
} else if (typeof ctaSource === 'string') {
let el = null;
try {
el = document.getElementById(ctaSource);
} catch (e) {}
if (!el && /^[a-zA-Z_-][a-zA-Z0-9_-]*$/.test(ctaSource)) {
try {
el = document.querySelector('.' + ctaSource);
} catch (e) {}
}
if (!el) {
try {
el = document.querySelector(`[data-cta="${ctaSource.replace(/"/g, '\\"')}"]`);
} catch (e) {}
}
if (!el && ctaSource === 'tour_footer_cta') {
try {
el = document.querySelector('.tour-footer-btn');
} catch (e) {}
}
if (el && el.innerText) {
resolvedSource = el.innerText.replace(/\s+/g, ' ').trim();
} else {
resolvedSource = ctaSource;
}
} else {
resolvedSource = String(ctaSource);
}
}
activeCtaSource = resolvedSource;
const isEnglish = window.location.pathname.includes('/en/');
let programKey = null;
if (ctaSource && typeof ctaSource === 'string') {
if (ctaSource.startsWith('pathfinder-')) {
programKey = ctaSource.replace('pathfinder-', '');
} else if (['IDEAS01', 'IDEAS02', 'IDEAS03', 'IDEAS04', 'IDEAS05', 'IDEAS06', 'IDEAS07'].includes(ctaSource)) {
programKey = ctaSource;
}
}
if (!programKey) {
const path = window.location.pathname.toLowerCase();
if (path.includes('mscai')) {
programKey = 'IDEAS04';
} else if (path.includes('mbainai')) {
programKey = 'IDEAS05';
} else if (path.includes('emba')) {
programKey = 'IDEAS03';
} else if (/(?:^|\/)mba(?:\.html|\/|$)/.test(path)) {
programKey = 'IDEAS02';
} else if (path.includes('fullbba')) {
programKey = 'IDEAS01';
} else if (path.includes('bba')) {
programKey = 'IDEAS07';
} else if (path.includes('dual-dba') || path.includes('dba')) {
programKey = 'IDEAS06';
}
}
const titlesVN = {
'IDEAS01': 'Cử nhân Quản trị Kinh doanh BBA',
'IDEAS07': 'Liên thông Cử nhân Thụy Sĩ',
'IDEAS02': 'MBA Thụy Sĩ',
'IDEAS03': 'EMBA Thụy Sĩ',
'IDEAS05': 'MBA in AI Thụy Sĩ',
'IDEAS04': 'MSc AI Thụy Sĩ',
'IDEAS06': 'Song bằng Tiến sĩ DBA',
'default': 'Chương trình Đào tạo Quốc tế'
};
const titlesEN = {
'IDEAS01': 'Bachelor of Business Administration (BBA)',
'IDEAS07': 'Swiss Bachelor\'s Top-up',
'IDEAS02': 'MBA Switzerland',
'IDEAS03': 'EMBA Switzerland',
'IDEAS05': 'Swiss MBA in AI',
'IDEAS04': 'Swiss MSc AI',
'IDEAS06': 'Song Doctoral degree DBA',
'default': 'International Programs'
};
let pageTitleText = null;
if (!programKey) {
const path = window.location.pathname.toLowerCase();
if (path.includes('so-do-to-chuc')) {
pageTitleText = isEnglish ? 'Organizational Structure' : 'Cơ Cấu Tổ Chức';
} else if (path.includes('he-thong-ho-tro-hoc-tap-lms-ideas')) {
pageTitleText = isEnglish ? 'LMS Ecosystem' : 'Hệ Sinh Thái LMS';
} else if (path.includes('ideas-talk')) {
pageTitleText = isEnglish ? 'IDEAS Talk & AI' : 'IDEAS Talk & AI';
} else if (path.includes('podcast')) {
pageTitleText = isEnglish ? 'Podcast Series 01' : 'Podcast Series 01';
} else if (path.includes('ambassador')) {
pageTitleText = isEnglish ? 'IDEAS Ambassador' : 'Đại Sứ IDEAS';
} else if (path.includes('sacombank')) {
pageTitleText = isEnglish ? '0% Installment Plan' : 'Trả Góp Học Phí';
} else if (path.includes('cac-khoan-chi-phi') || path.includes('hoc-phi')) {
pageTitleText = isEnglish ? 'Tuition & Fees' : 'Học Phí & Ưu Đãi';
} else if (path.includes('dong-su-kien')) {
pageTitleText = isEnglish ? 'IDEAS Events' : 'Sự Kiện IDEAS';
}
}
const titleText = pageTitleText || (isEnglish
? (titlesEN[programKey] || titlesEN['default'])
: (titlesVN[programKey] || titlesVN['default']));
const modalTitleEl = regModal.querySelector('#modal-program-title') ||
regModal.querySelector('.modal-form-header h3 span.gradient-text') ||
regModal.querySelector('.modal-form-header h3 span') ||
regModal.querySelector('.modal-form-header h3');
if (modalTitleEl) {
modalTitleEl.innerText = titleText;
}
const modalForm = document.getElementById('modal-cta-form');
const modalSuccess = document.getElementById('modal-form-success');
if (modalForm) modalForm.style.display = 'grid';
if (modalSuccess) modalSuccess.style.display = 'none';
regModal.style.display = 'flex';
setTimeout(() => {
regModal.classList.add('open');
regModal.setAttribute('aria-hidden', 'false');
lockScroll();
}, 10);
}
function closeRegModal() {
if (!regModal) return;
regModal.classList.remove('open');
regModal.setAttribute('aria-hidden', 'true');
setTimeout(() => {
regModal.style.display = 'none';
unlockScroll();
}, 400);
}
window.openRegModal = openRegModal;
window.closeRegModal = closeRegModal;
if (regClose) regClose.addEventListener('click', closeRegModal);
if (regOverlay) regOverlay.addEventListener('click', closeRegModal);
document.querySelectorAll('a[href="#dang-ky"]').forEach(cta => {
cta.addEventListener('click', function (e) {
e.preventDefault();
e.stopPropagation();
openRegModal(cta);
});
});
document.querySelectorAll('.pricing-card').forEach(card => {
card.addEventListener('mouseenter', () => {
if (!card.classList.contains('pricing-featured')) {
card.style.borderColor = 'rgba(99,102,241,0.3)';
}
});
card.addEventListener('mouseleave', () => {
if (!card.classList.contains('pricing-featured')) {
card.style.borderColor = '';
}
});
});
const compCards = document.querySelectorAll('.competency-card');
if ('IntersectionObserver' in window && compCards.length > 0) {
const compObserver = new IntersectionObserver(
(entries) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
const allCards = [...document.querySelectorAll('.competency-card')];
const idx = allCards.indexOf(entry.target);
entry.target.style.transitionDelay = `${idx * 90}ms`;
entry.target.classList.add('comp-visible');
compObserver.unobserve(entry.target);
}
});
},
{ threshold: 0.15 }
);
const style = document.createElement('style');
style.textContent = `
.competency-card { opacity: 0; transform: translateY(28px); }
.competency-card.comp-visible { opacity: 1; transform: translateY(0); transition: opacity 0.5s ease, transform 0.5s ease, border-color 0.3s ease, box-shadow 0.3s ease; }
`;
document.head.appendChild(style);
compCards.forEach(c => compObserver.observe(c));
}
const shakeStyle = document.createElement('style');
shakeStyle.textContent = `
@keyframes shake {
0%, 100% { transform: translateX(0); }
20% { transform: translateX(-6px); }
40% { transform: translateX(6px); }
60% { transform: translateX(-4px); }
80% { transform: translateX(4px); }
}
`;
document.head.appendChild(shakeStyle);
const lightbox = document.getElementById('lightbox');
const lightboxImg = document.getElementById('lightbox-img');
const lightboxClose = document.getElementById('lightbox-close');
function openLightbox(src, alt) {
if (!lightbox || !lightboxImg) return;
lightboxImg.src = src;
lightboxImg.alt = alt || '';
lightbox.classList.add('open');
lockScroll();
if (lightboxClose) lightboxClose.focus();
}
function closeLightbox() {
if (!lightbox) return;
lightbox.classList.remove('open');
unlockScroll();
if (lightboxImg) lightboxImg.src = '';
}
document.querySelectorAll('.lightbox-trigger').forEach(trigger => {
trigger.addEventListener('click', (e) => {
e.preventDefault();
const imgSrc = trigger.dataset.img || trigger.getAttribute('href');
const imgEl = trigger.querySelector('img');
const alt = imgEl ? imgEl.alt : '';
openLightbox(imgSrc, alt);
});
});
if (lightboxClose) lightboxClose.addEventListener('click', closeLightbox);
if (lightbox) {
lightbox.addEventListener('click', (e) => {
if (e.target === lightbox) closeLightbox();
});
}
const accredData = isEn ? {
"qs": {
title: "QS Stars 5 Stars Overall",
desc: "SWISS UMEF has been awarded the “QS Stars 5 Stars Overall” rating by Quacquarelli Symonds (QS), one of the most prestigious university ranking systems in the world. These international rankings highlight Swiss UMEF's excellence across multiple dimensions, including teaching quality, employability, online learning, social impact, diversity, and academic development, reaffirming its position as a leading institution committed to innovation, inclusion, and global engagement."
},
"sac": {
title: "Swiss Accreditation Council",
desc: "The highest federal accreditation authority in Switzerland, responsible for supervising and accrediting quality assurance organizations in Swiss higher education. SAC applies common standards (ESG) for quality assurance. SWISS UMEF is the first private higher education institution in Geneva to be fully accredited by the Swiss Accreditation Council."
},
"eduqua": {
title: "Swiss Quality Label (EduQua)",
desc: "A Swiss quality label for continuing education, recognized by the Swiss Government. EduQua assesses quality based on six criteria: objectives and strategy, course offerings, information and advice, learners and teachers, quality management systems, and the use of resources. UMEF is EduQua certified."
},
"iacbe": {
title: "International Accreditation Council for Business Education (IACBE)",
desc: "An international accreditation organization for business education recognized by the Council for Higher Education Accreditation (CHEA). IACBE evaluates business education quality based on mission, goals, resources, processes, and outcomes. UMEF programs (BBA, MBA, DBA) are accredited by IACBE."
},
"chea": {
title: "Council for Higher Education Accreditation (CHEA)",
desc: "A non-governmental organization that advocates for and promotes quality assurance in higher education. CHEA recognizes prestigious accreditation bodies and ensures universities adhere to quality standards. UMEF is recognized via IACBE, a CHEA-approved business education accreditor."
},
"acbsp": {
title: "Accreditation Council for Business Schools and Programs (ACBSP)",
desc: "A global business education accrediting body recognized by the US Department of Education. ACBSP applies rigorous standards focusing on teaching excellence, student learning outcomes, and continuous quality improvement. ACBSP is also a member of CHEA and collaborates with IACBE."
}
} : {
"qs": {
title: "QS Stars 5 Stars Overall",
desc: "SWISS UMEF đã được Quacquarelli Symonds (QS), một trong những hệ thống đánh giá đại học uy tín nhất thế giới trao tặng Xếp hạng “QS Stars 5 Stars Overall”. Các bảng xếp hạng quốc tế này nêu bật sự xuất sắc của Swiss UMEF trên nhiều khía cạnh, bao gồm chất lượng giảng dạy, khả năng tuyển dụng, học tập trực tuyến, tác động xã hội, sự đa dạng và phát triển học thuật, tái khẳng định vị thế là một tổ chức hàng đầu cam kết đổi mới, hòa nhập và tham gia toàn cầu."
},
"sac": {
title: "Swiss Accreditation Council",
desc: "Là cơ quan kiểm định liên bang cao nhất Thụy Sĩ với nhiệm vụ giám sát và công nhận các tổ chức kiểm định chất lượng giáo dục cao học ở Thụy Sĩ. SAC áp dụng các tiêu chuẩn chung cho các tổ chức kiểm định và các trường đại học về đảm bảo chất lượng trong giáo dục cao học (ESG). SWISS UMEF là tổ chức giáo dục đại học tư nhân đầu tiên ở Geneva được công nhận bởi cơ quan kiểm định liên bang cao nhất của Thụy Sĩ."
},
"eduqua": {
title: "Nhãn chất lượng Thụy Sĩ (EduQua)",
desc: "Là một nhãn chất lượng Thụy Sĩ cho giáo dục thường xuyên, được Chính phủ Thụy Sĩ công nhận. EduQua đánh giá chất lượng giáo dục thường xuyên dựa trên sáu tiêu chuẩn: mục tiêu và chiến lược, cung cấp khóa học, thông tin và tư vấn, người học và người giảng dạy, hệ thống quản lý chất lượng và việc sử dụng hiệu quả nguồn lực. UMEF đã được EduQua kiểm định và công nhận cho các khóa học."
},
"iacbe": {
title: "International Accreditation Council for Business Education (IACBE)",
desc: "Là một tổ chức kiểm định chất lượng giáo dục kinh doanh quốc tế, được Hội đồng Kiểm định Giáo dục Đại học (CHEA) công nhận. IACBE đánh giá chất lượng giáo dục kinh doanh dựa trên các tiêu chí về mục tiêu, nhiệm vụ, chiến lược, nguồn lực, quy trình và kết quả của các trường đại học. UMEF đã được IACBE kiểm định và công nhận cho các chương trình Bachelor of Business Administration (BBA), Master of Business Administration (MBA) và Doctor of Business Administration (DBA)."
},
"chea": {
title: "Council for Higher Education Accreditation (CHEA)",
desc: "Là một tổ chức phi chính phủ có nhiệm vụ bảo vệ và thúc đẩy chất lượng giáo dục đại học ở Hoa Kỳ. CHEA công nhận các tổ chức kiểm định chất lượng giáo dục đại học có uy tín và đảm bảo rằng các trường đại học tuân thủ các tiêu chuẩn chất lượng. UMEF được CHEA công nhận thông qua IACBE và IACBE là tổ chức kiểm định uy tín, các tiêu chuẩn được đặt ra để chuyên về các trường Business, một trong những tổ chức kiểm định được CHEA chấp thuận."
},
"acbsp": {
title: "Accreditation Council for Business Schools and Programs (ACBSP)",
desc: "Là một tổ chức kiểm định chất lượng các trường và các chương trình đào tạo trong lĩnh vực kinh doanh được Bộ Giáo dục Hoa Kỳ công nhận. ACBSP áp dụng các tiêu chuẩn nghiêm ngặt về chương trình giảng dạy, đội ngũ giảng viên, cơ sở vật chất, kết quả học tập và liên kết với doanh nghiệp. ACBSP cũng là thành viên của Hội đồng Kiểm định Giáo dục Đại học (CHEA) và tham gia vào mạng lưới kiểm định giáo dục kinh doanh quốc tế (IACBE)."
},
};
const accredModal = document.getElementById('accred-modal');
const accredTitle = document.getElementById('accred-title');
const accredDesc = document.getElementById('accred-desc');
const accredClose = document.getElementById('accred-modal-close');
const accredOverlay = document.getElementById('accred-modal-overlay');
function openAccredModal(key) {
const data = accredData[key];
if (!data || !accredModal) return;
accredTitle.textContent = data.title;
accredDesc.innerHTML = data.desc;
accredModal.classList.add('open');
accredModal.setAttribute('aria-hidden', 'false');
lockScroll();
}
function closeAccredModal() {
if (!accredModal) return;
accredModal.classList.remove('open');
accredModal.setAttribute('aria-hidden', 'true');
unlockScroll();
}
document.querySelectorAll('.accred-trigger').forEach(trigger => {
trigger.addEventListener('click', () => {
const key = trigger.dataset.accred;
openAccredModal(key);
});
});
if (accredClose) accredClose.addEventListener('click', closeAccredModal);
if (accredOverlay) accredOverlay.addEventListener('click', closeAccredModal);
document.addEventListener('keydown', (e) => {
if (e.key === 'Escape') {
closeLightbox();
closeAccredModal();
closeRegModal();
}
});
const stack = document.getElementById('accred-stack');
const prevBtn = document.getElementById('accred-prev');
const nextBtn = document.getElementById('accred-next');
if (stack) {
let isAnimating = false;
function rotateStack(direction = 'next') {
if (isAnimating) return;
isAnimating = true;
const cards = [...stack.querySelectorAll('.accred-stack-card')];
const numCards = cards.length;
if (direction === 'next') {
const topCard = stack.querySelector('.pos-1');
topCard.style.transition = 'all 0.6s cubic-bezier(0.23, 1, 0.32, 1)';
topCard.style.transform = 'translateX(120%) rotate(20deg) translateZ(100px)';
topCard.style.opacity = '0';
topCard.style.zIndex = '10';
setTimeout(() => {
cards.forEach(card => {
let currentPos = 1;
for (let i = 1; i <= numCards; i++) {
if (card.classList.contains(`pos-${i}`)) {
currentPos = i;
card.classList.remove(`pos-${i}`);
break;
}
}
let nextPos = currentPos === 1 ? numCards : currentPos - 1;
card.classList.add(`pos-${nextPos}`);
if (currentPos === 1) {
card.style.transition = 'none';
card.style.transform = '';
card.style.opacity = '';
card.style.zIndex = '';
}
});
setTimeout(() => {
cards.forEach(c => c.style.transition = '');
isAnimating = false;
}, 50);
}, 500);
} else {
const backCard = stack.querySelector(`.pos-${numCards}`);
backCard.style.transition = 'none';
backCard.style.transform = 'translateX(-120%) rotate(-15deg) translateZ(100px)';
backCard.style.opacity = '0';
backCard.style.zIndex = '10';
setTimeout(() => {
backCard.style.transition = 'all 0.6s cubic-bezier(0.23, 1, 0.32, 1)';
cards.forEach(card => {
let currentPos = 1;
for (let i = 1; i <= numCards; i++) {
if (card.classList.contains(`pos-${i}`)) {
currentPos = i;
card.classList.remove(`pos-${i}`);
break;
}
}
let nextPos = currentPos === numCards ? 1 : currentPos + 1;
card.classList.add(`pos-${nextPos}`);
if (currentPos === numCards) {
card.style.transform = '';
card.style.opacity = '';
card.style.zIndex = '';
}
});
setTimeout(() => {
cards.forEach(c => c.style.transition = '');
isAnimating = false;
}, 600);
}, 50);
}
}
if (nextBtn) nextBtn.addEventListener('click', () => rotateStack('next'));
if (prevBtn) prevBtn.addEventListener('click', () => rotateStack('prev'));
stack.addEventListener('click', () => rotateStack('next'));
}
function initMobileSliders() {
if (window.innerWidth <= 768) {
const mobileGrids = [];
const selectors = [
'.pain-cards',
'.competency-grid',
'#audience-cards-grid',
'.testimonials-grid',
'.gallery-mosaic',
'.video-talk-grid'
];
selectors.forEach(sel => {
document.querySelectorAll(sel).forEach(el => mobileGrids.push(el));
});
document.querySelectorAll('#giang-vien, #co-van').forEach(section => {
section.querySelectorAll('div').forEach(div => {
const styleAttr = div.getAttribute('style') || '';
if (styleAttr.includes('display:grid') || styleAttr.includes('display: grid') || div.style.display === 'grid') {
mobileGrids.push(div);
}
});
});
mobileGrids.forEach((grid, idx) => {
if (grid.dataset.sliderId) return;
let hasExistingDots = false;
document.querySelectorAll('.scroll-dots').forEach(dotsContainer => {
const targetSel = dotsContainer.dataset.scrollTarget;
if (targetSel) {
try {
if (grid.matches(targetSel) || grid.closest(targetSel) === grid) {
hasExistingDots = true;
}
} catch (e) {
}
}
});
if (hasExistingDots) {
grid.dataset.sliderId = 'manual-slider';
return;
}
const id = `slider-target-${idx}`;
grid.dataset.sliderId = id;
grid.classList.add(id);
const dotsDiv = document.createElement('div');
dotsDiv.className = 'scroll-dots';
dotsDiv.dataset.scrollTarget = `.${id}`;
grid.parentNode.insertBefore(dotsDiv, grid.nextSibling);
});
}
}
function initScrollDots() {
const dotContainers = document.querySelectorAll('.scroll-dots');
dotContainers.forEach(container => {
const targetSelector = container.dataset.scrollTarget;
const target = document.querySelector(targetSelector);
if (!target) return;
const items = [...target.children].filter(child => !child.classList.contains('scroll-dots'));
const itemCount = items.length;
if (itemCount === 0) return;
container.innerHTML = '';
for (let i = 0; i < itemCount; i++) {
const dot = document.createElement('div');
dot.classList.add('scroll-dot');
if (i === 0) dot.classList.add('active');
dot.addEventListener('click', () => {
const offset = (target.offsetWidth - items[i].offsetWidth) / 2;
const targetPos = (items[i].offsetLeft - target.offsetLeft) - offset;
target.scrollTo({ left: targetPos, behavior: 'smooth' });
});
container.appendChild(dot);
}
target.addEventListener('scroll', () => {
const scrollLeft = target.scrollLeft;
const containerWidth = target.offsetWidth;
let activeIndex = 0;
let minDiff = Infinity;
items.forEach((item, idx) => {
const itemCenter = item.offsetLeft - target.offsetLeft + (item.offsetWidth / 2);
const scrollCenter = scrollLeft + (containerWidth / 2);
const diff = Math.abs(itemCenter - scrollCenter);
if (diff < minDiff) {
minDiff = diff;
activeIndex = idx;
}
});
const dots = container.querySelectorAll('.scroll-dot');
dots.forEach((dot, idx) => {
dot.classList.toggle('active', idx === activeIndex);
});
}, { passive: true });
});
}
function initCustomSelects() {
const selects = document.querySelectorAll('.cta-form select');
selects.forEach(select => {
if (select.parentNode.classList.contains('select-wrapper')) return;
const wrapper = document.createElement('div');
wrapper.className = 'select-wrapper';
select.parentNode.insertBefore(wrapper, select);
wrapper.appendChild(select);
const trigger = document.createElement('div');
trigger.className = 'select-trigger';
const selectedText = select.options[select.selectedIndex]?.text || 'Chọn một tùy chọn';
trigger.innerHTML = `<span class="select-value">${selectedText}</span>`;
wrapper.appendChild(trigger);
const dropdown = document.createElement('div');
dropdown.className = 'select-dropdown';
[...select.options].forEach((option, idx) => {
const optDiv = document.createElement('div');
optDiv.className = 'select-option';
if (select.selectedIndex === idx) optDiv.classList.add('selected');
optDiv.textContent = option.text;
optDiv.addEventListener('click', (e) => {
e.stopPropagation();
select.selectedIndex = idx;
trigger.querySelector('.select-value').textContent = option.text;
dropdown.querySelectorAll('.select-option').forEach(o => o.classList.remove('selected'));
optDiv.classList.add('selected');
dropdown.classList.remove('show');
trigger.classList.remove('active');
select.dispatchEvent(new Event('change', { bubbles: true }));
select.dispatchEvent(new Event('input', { bubbles: true }));
});
dropdown.appendChild(optDiv);
});
wrapper.appendChild(dropdown);
trigger.addEventListener('click', (e) => {
e.stopPropagation();
document.querySelectorAll('.select-dropdown.show').forEach(d => {
if (d !== dropdown) {
d.classList.remove('show');
d.previousElementSibling.classList.remove('active');
}
});
const isOpening = !dropdown.classList.contains('show');
dropdown.classList.toggle('show');
trigger.classList.toggle('active');
});
});
document.addEventListener('click', () => {
document.querySelectorAll('.select-dropdown.show').forEach(d => {
d.classList.remove('show');
d.previousElementSibling.classList.remove('active');
});
});
}
function initBackToTop() {
if (document.querySelector('.back-to-top-floating')) return;
document.querySelectorAll('.back-to-top-btn').forEach(el => {
el.style.setProperty('display', 'none', 'important');
});
const style = document.createElement('style');
style.textContent = `
.back-to-top-btn, .ideas_footer .back-to-top-btn {
display: none !important;
}
.back-to-top-floating {
position: fixed;
bottom: 28px;
left: 50%;
transform: translateX(-50%) translateY(20px);
background: #ab0e00;
color: #ffffff;
border: none;
border-radius: 9999px;
padding: 10px 22px;
font-size: 0.9rem;
font-weight: 700;
cursor: pointer;
box-shadow: 0 4px 16px rgba(171, 14, 0, 0.3);
z-index: 99;
opacity: 0;
pointer-events: none;
transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.3s ease, background-color 0.2s ease, box-shadow 0.2s ease;
display: flex;
align-items: center;
justify-content: center;
gap: 8px;
white-space: nowrap;
}
.back-to-top-floating.visible {
opacity: 1;
pointer-events: auto;
transform: translateX(-50%) translateY(0);
}
.back-to-top-floating.visible:hover {
background-color: #900c00;
transform: translateX(-50%) translateY(-4px);
box-shadow: 0 8px 24px rgba(171, 14, 0, 0.4);
}
.back-to-top-floating.visible:active {
transform: translateX(-50%) translateY(1px);
}
@media (max-width: 768px) {
.back-to-top-floating {
bottom: 28px;
padding: 8px 18px;
font-size: 0.85rem;
}
}
`;
document.head.appendChild(style);
const btn = document.createElement('button');
btn.className = 'back-to-top-floating';
btn.setAttribute('aria-label', isEn ? 'Back to top' : 'Lên đầu trang');
btn.innerHTML = `
<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
<polyline points="18 15 12 9 6 15"></polyline>
</svg>
<span>${isEn ? 'Back to top' : 'Lên đầu trang'}</span>
`;
document.body.appendChild(btn);
function checkScroll() {
const scrollableHeight = document.documentElement.scrollHeight - window.innerHeight;
if (scrollableHeight <= 0) return;
const scrollPercent = window.scrollY / scrollableHeight;
if (scrollPercent >= 0.3) {
btn.classList.add('visible');
} else {
btn.classList.remove('visible');
}
}
let isBackToTopThrottled = false;
window.addEventListener('scroll', () => {
if (!isBackToTopThrottled) {
isBackToTopThrottled = true;
requestAnimationFrame(() => {
checkScroll();
isBackToTopThrottled = false;
});
}
}, { passive: true });
btn.addEventListener('click', () => {
window.scrollTo({ top: 0, behavior: 'smooth' });
});
}
function initMobileDropdowns() {
const dropdowns = document.querySelectorAll('.mobile-dropdown');
dropdowns.forEach(dropdown => {
const toggleBtn = dropdown.querySelector('.mobile-dropdown-toggle');
if (toggleBtn) {
toggleBtn.addEventListener('click', (e) => {
e.preventDefault();
e.stopPropagation();
const isExpanded = toggleBtn.getAttribute('aria-expanded') === 'true';
toggleBtn.setAttribute('aria-expanded', !isExpanded);
dropdown.classList.toggle('expanded');
});
}
const links = dropdown.querySelectorAll('.mobile-dropdown-item, .mobile-dropdown-item-simple');
links.forEach(link => {
link.addEventListener('click', () => {
if (toggleBtn) {
toggleBtn.setAttribute('aria-expanded', 'false');
}
dropdown.classList.remove('expanded');
const mobileMenu = document.getElementById('mobile-menu');
const overlay = document.getElementById('mobile-overlay');
const hamburger = document.getElementById('hamburger');
if (mobileMenu) {
mobileMenu.classList.remove('open');
mobileMenu.setAttribute('aria-hidden', 'true');
}
if (overlay) overlay.classList.remove('open');
if (hamburger) hamburger.classList.remove('active');
});
});
});
}
function initialize() {
initBackToTop();
initMobileDropdowns();
initCustomSelects();
initMobileSliders();
initScrollDots();
}
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', initialize);
} else {
initialize();
}
window.initBackToTop = initBackToTop;
window.addEventListener('resize', () => {
initMobileSliders();
initScrollDots();
});
console.log('%cIDEAS × Swiss UMEF MBA [Portal]', 'background:#ab0e00;color:#fff;padding:4px 12px;border-radius:4px;font-weight:700');
})();
function toggleAccredSection() {
const content = document.getElementById('accred-expandable-content');
const btn = document.getElementById('accred-toggle-btn');
const btnText = document.getElementById('accred-toggle-btn-text');
const btnIcon = document.getElementById('accred-toggle-btn-icon');
if (!content || !btnText) return;
let originalText = btnText.getAttribute('data-original-text');
if (!originalText) {
originalText = btnText.textContent.trim();
btnText.setAttribute('data-original-text', originalText);
}
if (content.classList.contains('is-open')) {
content.classList.remove('is-open');
btnText.textContent = originalText;
if (btnIcon) btnIcon.style.transform = 'rotate(0deg)';
} else {
content.classList.add('is-open');
btnText.textContent = isEn ? 'Hide Details' : 'Thu gọn thông tin';
if (btnIcon) btnIcon.style.transform = 'rotate(180deg)';
}
}
function toggleTripSection() {
const content = document.getElementById('trip-expandable-content');
const btn = document.getElementById('trip-toggle-btn');
const btnText = document.getElementById('trip-toggle-btn-text');
const btnIcon = document.getElementById('trip-toggle-btn-icon');
if (!content || !btnText) return;
let originalText = btnText.getAttribute('data-original-text');
if (!originalText) {
originalText = btnText.textContent.trim();
btnText.setAttribute('data-original-text', originalText);
}
if (content.classList.contains('is-open')) {
content.classList.remove('is-open');
btnText.textContent = originalText;
if (btnIcon) btnIcon.style.transform = 'rotate(0deg)';
} else {
content.classList.add('is-open');
btnText.textContent = isEn ? 'Hide Details' : 'Thu gọn thông tin';
if (btnIcon) btnIcon.style.transform = 'rotate(180deg)';
}
}
let capitalsSequenceStarted = false;
function initTourModalSequence() {
const modalScrollable = document.getElementById('tour-modal-scrollable');
if (!modalScrollable) return;
const observer = new IntersectionObserver((entries) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
entry.target.classList.add('is-visible');
if (entry.target.id === 'tour-capitals-section' && !capitalsSequenceStarted) {
capitalsSequenceStarted = true;
startCapitalsSequence(entry.target);
}
if (entry.target.id === 'tour-video-container') {
const video = document.getElementById('tour-video-el');
if (video) {
video.play().catch(e => console.log('Autoplay blocked'));
}
}
}
});
}, {
threshold: 0.15,
root: modalScrollable
});
modalScrollable.querySelectorAll('.tour-reveal').forEach(el => observer.observe(el));
const capitalsSec = document.getElementById('tour-capitals-section');
if (capitalsSec) observer.observe(capitalsSec);
const videoCont = document.getElementById('tour-video-container');
if (videoCont) observer.observe(videoCont);
}
function startCapitalsSequence(section) {
const cards = section.querySelectorAll('.tour-capital-card');
const footer = section.querySelector('.tour-capitals-footer');
let step = 0;
function nextStep() {
step++;
if (step <= cards.length) {
cards.forEach((card, idx) => {
const cardStep = idx + 1;
if (cardStep === step) {
card.classList.add('is-visible');
card.classList.add('is-focused');
} else {
card.classList.remove('is-focused');
if (cardStep < step) {
card.classList.add('is-previous');
}
}
});
setTimeout(nextStep, 1000);
} else {
cards.forEach(card => {
card.classList.remove('is-focused');
card.classList.remove('is-previous');
card.classList.add('is-all-active');
});
if (footer) footer.classList.add('is-visible');
}
}
nextStep();
}
function openTourModal() {
const modal = document.getElementById('tour-modal');
if (!modal) return;
modal.style.display = 'flex';
const modalScrollable = document.getElementById('tour-modal-scrollable');
if (modalScrollable) {
modalScrollable.scrollTop = 0;
}
if (typeof window.lockScroll === 'function') {
window.lockScroll();
}
capitalsSequenceStarted = false;
setTimeout(() => {
modal.classList.add('open');
initTourModalSequence();
}, 10);
}
function closeTourModal() {
const modal = document.getElementById('tour-modal');
if (!modal) return;
modal.classList.remove('open');
if (typeof window.unlockScroll === 'function') {
window.unlockScroll();
}
const video = document.getElementById('tour-video-el');
if (video) video.pause();
setTimeout(() => {
modal.style.display = 'none';
const modalScrollable = document.getElementById('tour-modal-scrollable');
if (modalScrollable) {
modalScrollable.scrollTop = 0;
}
const videoContainer = document.getElementById('tour-video-container');
const fallbackGrid = document.getElementById('tour-images-fallback');
if (videoContainer) {
videoContainer.style.display = 'block';
videoContainer.style.opacity = '1';
videoContainer.style.transform = 'scale(1)';
}
if (fallbackGrid) {
fallbackGrid.style.display = 'none';
fallbackGrid.classList.remove('is-visible');
}
const capitalsSec = document.getElementById('tour-capitals-section');
if (capitalsSec) {
capitalsSec.querySelectorAll('.tour-capital-card').forEach(card => {
card.classList.remove('is-visible', 'is-focused', 'is-previous', 'is-all-active');
});
const footer = capitalsSec.querySelector('.tour-capitals-footer');
if (footer) footer.classList.remove('is-visible');
}
const revealItems = document.getElementById('tour-modal-scrollable').querySelectorAll('.tour-reveal');
revealItems.forEach(item => item.classList.remove('is-visible'));
}, 500);
}
function openBookingModalFromTour(btn) {
closeTourModal();
setTimeout(() => {
if (typeof window.openRegModal === 'function') {
window.openRegModal(btn || 'tour_footer_cta');
}
}, 300);
}
document.addEventListener('DOMContentLoaded', () => {
document.querySelectorAll('#trip-cta').forEach(btn => {
btn.addEventListener('click', (e) => {
e.preventDefault();
e.stopPropagation();
openTourModal();
});
});
const closeBtn = document.getElementById('tour-modal-close');
const overlay = document.getElementById('tour-modal-overlay');
if (closeBtn) closeBtn.addEventListener('click', closeTourModal);
if (overlay) overlay.addEventListener('click', closeTourModal);
const video = document.getElementById('tour-video-el');
const playBtn = document.getElementById('tour-video-play-btn');
const muteBtn = document.getElementById('tour-video-mute-btn');
const videoContainer = document.getElementById('tour-video-container');
const fallbackGrid = document.getElementById('tour-images-fallback');
if (video) {
video.addEventListener('ended', () => {
if (videoContainer) {
videoContainer.style.opacity = '0';
videoContainer.style.transform = 'scale(0.95)';
setTimeout(() => {
videoContainer.style.display = 'none';
if (fallbackGrid) {
fallbackGrid.style.display = 'grid';
setTimeout(() => {
fallbackGrid.classList.add('is-visible');
}, 50);
}
}, 800);
}
});
if (playBtn) {
playBtn.addEventListener('click', (e) => {
e.stopPropagation();
if (video.paused) {
video.play().catch(err => console.log(err));
playBtn.innerHTML = '<svg width="24" height="24" fill="currentColor" viewBox="0 0 24 24"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z" /></svg>';
} else {
video.pause();
playBtn.innerHTML = '<svg width="24" height="24" fill="currentColor" viewBox="0 0 24 24"><path d="M8 5v14l11-7z" /></svg>';
}
});
}
if (muteBtn) {
muteBtn.addEventListener('click', (e) => {
e.stopPropagation();
video.muted = !video.muted;
if (video.muted) {
muteBtn.innerHTML = '<svg width="20" height="20" fill="currentColor" viewBox="0 0 24 24"><path d="M16.5 12c0-1.77-1.02-3.29-2.5-4.03v2.21l2.45 2.45c.03-.2.05-.41.05-.63zm2.5 0c0 .94-.2 1.82-.54 2.64l1.51 1.51C20.63 14.91 21 13.5 21 12c0-4.28-2.99-7.86-7-8.77v2.06c2.87.86 5 3.54 5 6.71zM4.27 3L3 4.27 7.73 9H3v6h4l5 5V9l-1.73-1.73L4.27 3zm10.73 7l-2-2v4l2-2z" /></svg>';
} else {
muteBtn.innerHTML = '<svg width="20" height="20" fill="currentColor" viewBox="0 0 24 24"><path d="M14 8.83v6.34L17.17 12 14 8.83zM12 4L7.03 9H3v6h4l5 5V4z" /></svg>';
}
});
}
}
});
document.addEventListener('keydown', (e) => {
if (e.key === 'Escape') {
closeTourModal();
}
});
function initDegreeModal() {
const wraps = document.querySelectorAll('.proof-degree-wrap');
if (wraps.length === 0) return;
wraps.forEach(w => {
w.style.cursor = 'pointer';
});
const modalHtml = `
<div class="degree-modal" id="degree-modal" role="dialog" aria-modal="true" aria-label="Xem mẫu văn bằng">
<div class="degree-modal-overlay" id="degree-modal-overlay"></div>
<div class="degree-modal-container">
<button class="degree-modal-close" id="degree-modal-close" aria-label="Đóng">✕</button>
<div class="degree-card-container" id="degree-card-container">
<div class="degree-card-inner" id="degree-card-inner">
<div class="degree-card-front">
<img id="degree-modal-img-front" src="" alt="Mặt trước văn bằng" />
</div>
<div class="degree-card-back">
<img id="degree-modal-img-back" src="https://ideas.edu.vn/wp-content/uploads/2025/11/Sample_page-0002.webp" alt="Mặt sau văn bằng" />
</div>
</div>
</div>
<button class="degree-modal-flip-btn" id="degree-modal-flip-btn">
<svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5" style="margin-right: 6px; vertical-align: middle;">
<path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 1121.21 8H18.5" />
</svg>
<span id="degree-modal-flip-text">Lật mặt sau</span>
</button>
<div class="degree-modal-hint">Bấm vào văn bằng hoặc nút lật để xem mặt trước/sau</div>
</div>
</div>
`;
const tempDiv = document.createElement('div');
tempDiv.innerHTML = modalHtml.trim();
const modalEl = tempDiv.firstChild;
document.body.appendChild(modalEl);
const overlay = document.getElementById('degree-modal-overlay');
const closeBtn = document.getElementById('degree-modal-close');
const cardContainer = document.getElementById('degree-card-container');
const cardInner = document.getElementById('degree-card-inner');
const flipBtn = document.getElementById('degree-modal-flip-btn');
const flipText = document.getElementById('degree-modal-flip-text');
const imgFront = document.getElementById('degree-modal-img-front');
const imgBack = document.getElementById('degree-modal-img-back');
function openModal(src, customBack) {
imgFront.src = src;
let backSrc = customBack;
if (!backSrc) {
backSrc = 'https://ideas.edu.vn/wp-content/uploads/2025/11/Sample_page-0002.webp';
if (src.includes('RB-DBA') || src.includes('Estiam') || src.includes('RB-College')) {
backSrc = 'https://ideas.edu.vn/wp-content/uploads/2025/10/RB-DBA-2.webp';
} else if (src.includes('Ascencia')) {
backSrc = 'https://ideas.edu.vn/wp-content/uploads/2024/11/MBA-Ascencia-Diploma-EN-MOFA-2.webp';
}
}
if (imgBack) {
imgBack.src = backSrc;
}
cardInner.classList.remove('flipped');
flipText.textContent = isEn ? 'Flip Back' : 'Lật mặt sau';
modalEl.classList.add('open');
if (typeof window.lockScroll === 'function') {
window.lockScroll();
}
}
function closeModal() {
modalEl.classList.remove('open');
if (typeof window.unlockScroll === 'function') {
window.unlockScroll();
}
}
function toggleFlip() {
const isFlipped = cardInner.classList.toggle('flipped');
flipText.textContent = isEn
? (isFlipped ? 'Flip Front' : 'Flip Back')
: (isFlipped ? 'Lật mặt trước' : 'Lật mặt sau');
}
wraps.forEach(w => {
w.addEventListener('click', (e) => {
e.preventDefault();
const img = w.querySelector('.proof-degree-img');
if (img) {
const customBack = w.getAttribute('data-back') || img.getAttribute('data-back');
openModal(img.src, customBack);
}
});
});
closeBtn.addEventListener('click', closeModal);
overlay.addEventListener('click', closeModal);
cardContainer.addEventListener('click', (e) => {
e.stopPropagation();
toggleFlip();
});
flipBtn.addEventListener('click', (e) => {
e.stopPropagation();
toggleFlip();
});
document.addEventListener('keydown', (e) => {
if (e.key === 'Escape' && modalEl.classList.contains('open')) {
closeModal();
}
});
}
function initScrollSpy() {
const spySections = document.querySelectorAll('section[id]');
const navLinks = document.querySelectorAll('.header-nav .nav-link[href^="#"], .mobile-menu .nav-link[href^="#"]');
if ('IntersectionObserver' in window && spySections.length > 0) {
const spyObserver = new IntersectionObserver((entries) => {
entries.forEach(entry => {
if (entry.isIntersecting) {
const id = entry.target.getAttribute('id');
navLinks.forEach(link => {
const href = link.getAttribute('href');
const isActive = href === '#' + id;
link.classList.toggle('active', isActive);
});
}
});
}, {
rootMargin: '-30% 0px -60% 0px'
});
spySections.forEach(section => spyObserver.observe(section));
}
}
function initWorkshopTabs() {
const tabBtns = document.querySelectorAll('.workshop-tab-btn');
const tabContents = document.querySelectorAll('.workshop-tab-content');
if (tabBtns.length === 0 || tabContents.length === 0) return;
tabBtns.forEach(btn => {
btn.addEventListener('click', () => {
const targetTab = btn.dataset.tab;
tabBtns.forEach(b => b.classList.remove('active'));
tabContents.forEach(c => c.classList.remove('active'));
btn.classList.add('active');
const targetContent = document.getElementById(targetTab);
if (targetContent) targetContent.classList.add('active');
});
});
}
function initVnGradModal() {
if (!document.querySelector('link[href*="font-awesome"]')) {
const fontAwesomeLink = document.createElement('link');
fontAwesomeLink.rel = 'stylesheet';
fontAwesomeLink.href = 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css';
document.head.appendChild(fontAwesomeLink);
}
const tourFooterBtnWrap = document.querySelector('.tour-footer-btn-wrap');
if (!tourFooterBtnWrap) return;
let trigger = document.getElementById('vn-graduation-trigger');
if (!trigger) {
trigger = document.createElement('button');
trigger.id = 'vn-graduation-trigger';
trigger.className = 'tour-vn-ceremony-btn';
trigger.innerHTML = `
<i class="fa-solid fa-graduation-cap"></i>
<span>Tham dự Lễ tại Việt Nam</span>
`;
const closeBtnSecondary = tourFooterBtnWrap.querySelector('.tour-footer-btn-secondary');
if (closeBtnSecondary) {
tourFooterBtnWrap.insertBefore(trigger, closeBtnSecondary);
} else {
tourFooterBtnWrap.appendChild(trigger);
}
}
let modalEl = document.getElementById('vn-ceremony-modal');
if (!modalEl) {
const modalHtml = `
<div class="vn-ceremony-modal" id="vn-ceremony-modal" role="dialog" aria-modal="true" aria-label="Lễ tốt nghiệp tại Việt Nam">
<div class="vn-ceremony-overlay" id="vn-ceremony-overlay"></div>
<div class="vn-ceremony-container" data-lenis-prevent>
<div class="vn-ceremony-header">
<h3 class="vn-ceremony-title">Hành trình tốt nghiệp tại <span>Việt Nam</span></h3>
<button class="vn-ceremony-close" id="vn-ceremony-close" aria-label="Đóng modal">✕</button>
</div>
<div class="vn-ceremony-content" data-lenis-prevent>
<div class="vn-timeline">
<!-- Event 2: 27/12/2025 -->
<div class="vn-timeline-item">
<div class="vn-timeline-badge"></div>
<div class="vn-timeline-date">27/12/2025</div>
<div class="vn-card">
<div class="vn-card-img-wrap">
<img decoding="async"
src="https://ideas.edu.vn/wp-content/uploads/2026/01/ltn27122025.webp"
alt="Lễ tốt nghiệp 27/12/2025" loading="lazy">
</div>
<div class="vn-card-info">
<a href="https://www.facebook.com/ideas.edu.vn/posts/pfbid034nzCDGcFVfz54M62b4Yod9iJ3mMx2eVNMXB33PpDeDSw6Xw1cZsH4oucpX2TogDcl?locale=vi_VN"
target="_blank" rel="noopener" class="vn-card-title">Lễ tốt nghiệp 27/12/2025</a>
<div class="vn-card-meta">
<p><i class="fa-solid fa-graduation-cap"></i> MBA/EMBA &mdash; Swiss UMEF</p>
<p><i class="fa-solid fa-location-dot"></i> Tp. Hồ Chí Minh</p>
</div>
<a href="https://www.facebook.com/ideas.edu.vn/posts/pfbid034nzCDGcFVfz54M62b4Yod9iJ3mMx2eVNMXB33PpDeDSw6Xw1cZsH4oucpX2TogDcl?locale=vi_VN"
target="_blank" rel="noopener" class="vn-card-play"
aria-label="Xem chi tiết">
<svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"
viewBox="0 0 24 24">
<path stroke-linecap="round" stroke-linejoin="round"
d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
</svg>
</a>
</div>
</div>
</div>
<!-- Event 3: 26/10/2024 -->
<div class="vn-timeline-item">
<div class="vn-timeline-badge"></div>
<div class="vn-timeline-date">26/10/2024</div>
<div class="vn-card">
<div class="vn-card-img-wrap">
<img decoding="async"
src="https://ideas.edu.vn/wp-content/uploads/2024/10/Totnghiepumef.jpg"
alt="Lễ tốt nghiệp 26/10/2024" loading="lazy">
</div>
<div class="vn-card-info">
<a href="https://www.youtube.com/watch?si=eJDfqKWc4HxT_TmS&v=fBf5YcaMxDY&feature=youtu.be"
target="_blank" rel="noopener" class="vn-card-title">Lễ tốt nghiệp 26/10/2024</a>
<div class="vn-card-meta">
<p><i class="fa-solid fa-graduation-cap"></i> Swiss UMEF &mdash; MBA/EMBA</p>
<p><i class="fa-solid fa-location-dot"></i> IDEAS</p>
</div>
<a href="https://www.youtube.com/watch?si=eJDfqKWc4HxT_TmS&v=fBf5YcaMxDY&feature=youtu.be"
target="_blank" rel="noopener" class="vn-card-play"
aria-label="Xem chi tiết">
<svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"
viewBox="0 0 24 24">
<path stroke-linecap="round" stroke-linejoin="round"
d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
</svg>
</a>
</div>
</div>
</div>
</div>
</div>
</div>
</div>
`;
const tempDiv = document.createElement('div');
tempDiv.innerHTML = modalHtml.trim();
modalEl = tempDiv.firstChild;
document.body.appendChild(modalEl);
}
const overlay = modalEl.querySelector('#vn-ceremony-overlay');
const closeBtn = modalEl.querySelector('#vn-ceremony-close');
function openModal() {
modalEl.classList.add('open');
if (typeof window.lockScroll === 'function') {
window.lockScroll();
}
}
function closeModal() {
modalEl.classList.remove('open');
if (typeof window.unlockScroll === 'function') {
window.unlockScroll();
}
}
trigger.addEventListener('click', (e) => {
e.preventDefault();
e.stopPropagation();
openModal();
});
if (closeBtn) closeBtn.addEventListener('click', closeModal);
if (overlay) overlay.addEventListener('click', closeModal);
document.addEventListener('keydown', (e) => {
if (e.key === 'Escape' && modalEl.classList.contains('open')) {
closeModal();
}
});
}
function initLanguageSwitcher() {
const langBtns = document.querySelectorAll('.lang-dropdown-item, .mobile-lang-btn');
langBtns.forEach(btn => {
btn.addEventListener('click', function (e) {
e.preventDefault();
const targetLang = this.getAttribute('data-lang');
if (!targetLang) return;
localStorage.setItem('lang', targetLang);
const currentLang = document.documentElement.lang || 'vi';
if (currentLang !== targetLang) {
let newPath;
const pathname = window.location.pathname;
if (targetLang === 'en') {
if (pathname.indexOf('/en/') === -1) {
const lastSlash = pathname.lastIndexOf('/');
const dir = pathname.substring(0, lastSlash + 1);
const filename = pathname.substring(lastSlash + 1);
newPath = dir + 'en/' + filename;
} else {
newPath = pathname;
}
} else {
const enIndex = pathname.indexOf('/en/');
if (enIndex !== -1) {
newPath = pathname.substring(0, enIndex) + '/' + pathname.substring(enIndex + 4);
} else {
newPath = pathname;
}
}
window.location.replace(newPath + window.location.search + window.location.hash);
}
});
});
}
function initWordPressBlog() {
const container = document.getElementById("wp-news-container");
if (!container) return;
async function loadWordPressPosts() {
try {
const response = await fetch("https://ideas.edu.vn/wp-json/wp/v2/posts?_embed&per_page=6");
if (!response.ok) throw new Error("API call failed");
const posts = await response.json();
if (posts && posts.length > 0) {
const featuredPost = posts[0];
const listPosts = posts.slice(1, 6);
const isEn = document.documentElement.lang === 'en';
const fTitle = featuredPost.title.rendered;
const fLink = featuredPost.link;
const fExcerpt = featuredPost.excerpt.rendered.replace(/<[^>]*>/g, '').substring(0, 150) + "...";
const fDate = new Date(featuredPost.date).toLocaleDateString(isEn ? 'en-US' : 'vi-VN', {
day: '2-digit',
month: isEn ? 'short' : '2-digit'
});
const fThumb = featuredPost._embedded['wp:featuredmedia']
? featuredPost._embedded['wp:featuredmedia'][0].source_url
: 'https://ideas.edu.vn/wp-content/uploads/2025/07/ideas_side2.webp';
let html = `
<div class="news-featured">
<a href="${fLink}" target="_blank" class="featured-card">
<div class="thumb">
<img src="${fThumb}" alt="${fTitle}" loading="lazy">
<span class="date-badge">${fDate}</span>
</div>
<div class="content">
<h3>${fTitle}</h3>
<p>${fExcerpt}</p>
<span class="read-more">${isEn ? 'Read More' : 'Đọc tiếp'} <i class="fa-solid fa-arrow-right"></i></span>
</div>
</a>
</div>
<div class="news-list">
`;
listPosts.forEach(post => {
const lTitle = post.title.rendered;
const lLink = post.link;
const lDate = new Date(post.date).toLocaleDateString(isEn ? 'en-US' : 'vi-VN', {
day: '2-digit',
month: isEn ? 'short' : '2-digit',
year: 'numeric'
});
const lThumb = post._embedded['wp:featuredmedia']
? post._embedded['wp:featuredmedia'][0].source_url
: 'https://ideas.edu.vn/wp-content/uploads/2025/07/ideas_side2.webp';
html += `
<a href="${lLink}" target="_blank" class="news-item">
<div class="item-thumb">
<img src="${lThumb}" alt="${lTitle}" loading="lazy">
</div>
<div class="item-info">
<h4>${lTitle}</h4>
<span class="item-date">${lDate}</span>
</div>
</a>
`;
});
html += `</div>`;
container.innerHTML = html;
if (typeof initCardTiltGlow === 'function') initCardTiltGlow();
}
} catch (error) {
console.error("Error loading WordPress posts:", error);
const isEn = document.documentElement.lang === 'en';
container.innerHTML = `<p style="grid-column: 1/-1; text-align: center; color: #ef4444; font-weight: 500; padding: 40px 0;">${isEn ? 'Failed to load news posts. Please try again later.' : 'Không thể tải tin tức. Vui lòng thử lại sau.'}</p>`;
}
}
loadWordPressPosts();
}
function initCardTiltGlow() {
if (window.innerWidth < 992) return;
const selectors = '.prog-card-new, .activity-card, .video-talk-card, .pain-card-v2, .pain-stat-card, .featured-card';
document.querySelectorAll(selectors).forEach(card => {
if (!card.querySelector('.card-glow')) {
const glow = document.createElement('div');
glow.className = 'card-glow';
card.appendChild(glow);
}
card.style.position = 'relative';
card.style.transformStyle = 'preserve-3d';
const isProgCard = card.classList.contains('prog-card-new');
card.addEventListener('mousemove', e => {
const rect = card.getBoundingClientRect();
const x = e.clientX - rect.left;
const y = e.clientY - rect.top;
card.style.setProperty('--mouse-x', `${x}px`);
card.style.setProperty('--mouse-y', `${y}px`);
if (!isProgCard) {
const centerX = rect.width / 2;
const centerY = rect.height / 2;
const rotateX = ((centerY - y) / centerY) * 3.5;
const rotateY = ((x - centerX) / centerX) * 3.5;
card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.015, 1.015, 1.015)`;
}
});
card.addEventListener('mouseleave', () => {
if (!isProgCard) {
card.style.transform = '';
}
card.style.transition = 'all 0.5s cubic-bezier(0.25, 1, 0.5, 1)';
});
card.addEventListener('mouseenter', () => {
if (!isProgCard) {
card.style.transition = 'none';
}
});
});
}
function initCountersParallax() {
const bgs = document.querySelectorAll('.counters-bg');
if (bgs.length === 0) return;
function updateParallax() {
bgs.forEach(bg => {
const parent = bg.parentElement;
const rect = parent.getBoundingClientRect();
if (rect.top < window.innerHeight && rect.bottom > 0) {
const speed = 0.25;
const viewportCenter = window.innerHeight / 2;
const sectionCenter = rect.top + rect.height / 2;
const yPos = (sectionCenter - viewportCenter) * speed;
bg.style.transform = `translate3d(0, ${yPos}px, 0) scale(1.15)`;
}
});
}
window.addEventListener('scroll', () => {
window.requestAnimationFrame(updateParallax);
}, { passive: true });
updateParallax();
}
function initCopyrightYear() {
document.querySelectorAll('.copyright-year').forEach(el => {
el.textContent = new Date().getFullYear();
});
}
function initReadingProgressAndSideNav() {
const header = document.getElementById('site-header');
if (header && !document.querySelector('.reading-progress-container')) {
const progContainer = document.createElement('div');
progContainer.className = 'reading-progress-container';
progContainer.innerHTML = '<div class="reading-progress-bar" id="reading-progress"></div>';
header.appendChild(progContainer);
}
window.addEventListener('scroll', () => {
const winScroll = document.documentElement.scrollTop || document.body.scrollTop;
const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
const scrolled = height > 0 ? (winScroll / height) * 100 : 0;
const progressEl = document.getElementById('reading-progress');
if (progressEl) {
progressEl.style.width = scrolled + '%';
}
}, { passive: true });
return;
const isEnglish = window.location.pathname.includes('/en/');
const sections = [
{ id: 'trang-chu', nameVi: 'Trang chủ', nameEn: 'Home', icon: 'fa-house' },
{ id: 'thong-ke', nameVi: 'Thống kê', nameEn: 'Stats', icon: 'fa-chart-simple' },
{ id: 'gioi-thieu', nameVi: 'Giới thiệu', nameEn: 'About', icon: 'fa-circle-info' },
{ id: 'thach-thuc', nameVi: 'Thách thức', nameEn: 'Challenges', icon: 'fa-circle-exclamation' },
{ id: 'lo-trinh-phu-hop', nameVi: 'Lộ trình', nameEn: 'Pathfinder', icon: 'fa-route' },
{ id: 'he-thong-ho-tro', nameVi: 'Hỗ trợ', nameEn: 'Support', icon: 'fa-handshake-angle' },
{ id: 'hoat-dong', nameVi: 'Hoạt động', nameEn: 'Activities', icon: 'fa-calendar-days' },
{ id: 'tin-tuc', nameVi: 'Tin tức', nameEn: 'News', icon: 'fa-newspaper', altId: 'news' },
{ id: 'dang-ky', nameVi: 'Đăng ký', nameEn: 'Register', icon: 'fa-pen-to-square' }
];
const activeSections = sections.filter(sec => {
const el = document.getElementById(sec.id) || (sec.altId && document.getElementById(sec.altId));
return !!el;
});
if (activeSections.length === 0) return;
const navContainer = document.createElement('div');
navContainer.className = 'side-dot-nav';
activeSections.forEach(sec => {
const item = document.createElement('div');
item.className = 'side-dot-item';
const targetId = document.getElementById(sec.id) ? sec.id : sec.altId;
item.setAttribute('data-target', '#' + targetId);
const name = isEnglish ? sec.nameEn : sec.nameVi;
item.innerHTML = `
<div class="side-dot-circle"></div>
<div class="side-dot-tooltip">
<i class="fa-solid ${sec.icon}" style="margin-right: 6px; color: #ab0e00;"></i>
<span>${name}</span>
</div>
`;
item.addEventListener('click', (e) => {
e.preventDefault();
const targetEl = document.getElementById(targetId);
if (targetEl) {
const headerHeight = header ? header.offsetHeight : 72;
const offsetPosition = targetEl.offsetTop - headerHeight;
window.scrollTo({
top: offsetPosition,
behavior: 'smooth'
});
}
});
navContainer.appendChild(item);
});
document.body.appendChild(navContainer);
function updateSideNav() {
const scrollPos = window.scrollY || window.pageYOffset || document.documentElement.scrollTop;
if (scrollPos > 300) {
navContainer.classList.add('is-visible');
} else {
navContainer.classList.remove('is-visible');
}
let currentActiveId = '';
const headerHeight = header ? header.offsetHeight : 72;
activeSections.forEach(sec => {
const targetId = document.getElementById(sec.id) ? sec.id : sec.altId;
const targetEl = document.getElementById(targetId);
if (targetEl) {
const top = targetEl.offsetTop - headerHeight - 100;
const bottom = top + targetEl.offsetHeight;
if (scrollPos >= top && scrollPos < bottom) {
currentActiveId = targetId;
}
}
});
const dots = navContainer.querySelectorAll('.side-dot-item');
dots.forEach(dot => {
const target = dot.getAttribute('data-target');
if (target === '#' + currentActiveId) {
dot.classList.add('active');
} else {
dot.classList.remove('active');
}
});
}
window.addEventListener('scroll', updateSideNav, { passive: true });
updateSideNav();
}
function initMobileTopSheet() {
if (window.innerWidth > 767) return;
if (document.querySelector('.mobile-top-sheet')) return;
const path = window.location.pathname.toLowerCase();
const isEn = path.includes('/en/');
let programName = "";
let programSub = "";
let avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2026/06/Logo_IDEAS_Slg.webp";
if (path.includes("mbainai")) {
programName = "MBA in AI";
programSub = isEn ? "Swiss Applied AI Master's" : "Thạc sĩ QTKD Ứng dụng AI";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2026/06/mba_in_ai.webp";
} else if (path.includes("mscinai") || path.includes("mscai")) {
programName = "MSc AI";
programSub = isEn ? "Applied AI Master of Science" : "Thạc sĩ Khoa học Trí tuệ Nhân tạo";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2025/09/mscai.png.webp";
} else if (path.includes("emba")) {
programName = "Executive MBA";
programSub = isEn ? "Executive Master of Business Administration" : "Thạc sĩ Điều hành QTKD";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2025/09/emba.png.webp";
} else if (path.includes("fullbba")) {
programName = "Full BBA";
programSub = isEn ? "Bachelor of Business Administration" : "Cử nhân Quản trị Kinh doanh";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2026/06/online_bba.webp";
} else if (path.includes("bba")) {
programName = isEn ? "Top-up BBA" : "TOP-UP BBA";
programSub = isEn ? "Top-up Bachelor of Business Administration" : "Liên thông Cử nhân QTKD";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2026/02/TOPUP.webp";
} else if (path.includes("dual-dba") || path.includes("dba")) {
programName = "Dual DBA";
programSub = isEn ? "Dual Doctorate of Business Administration" : "Tiến sĩ QTKD song bằng";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2025/10/Dual-DBA.webp";
} else if (/(?:^|\/)mba(?:\.html|\/|$)/.test(path)) {
programName = "Online MBA";
programSub = isEn ? "Master of Business Administration" : "Thạc sĩ Quản trị Kinh doanh";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2025/09/online-mba-1.png.webp";
} else {
programName = isEn ? "IDEAS" : "IDEAS";
programSub = isEn ? "Admissions & Scholarship Roadmaps" : "Tư vấn lộ trình phù hợp";
avatarUrl = "https://ideas.edu.vn/wp-content/uploads/2026/06/Logo_IDEAS_Slg.webp";
}
const sheet = document.createElement('div');
sheet.className = 'mobile-top-sheet';
const btnText = isEn ? "Consult Now" : "Tư vấn";
sheet.innerHTML = `
<div class="mbs-left">
<div class="mbs-avatar-container">
<img src="${avatarUrl}" class="mbs-avatar" alt="${programName}">
</div>
<div class="mbs-info">
<div class="mbs-title">${programName}</div>
<div class="mbs-subtitle">${programSub}</div>
</div>
</div>
<button type="button" class="btn btn-primary mbs-action-btn">${btnText}</button>
`;
const style = document.createElement('style');
style.textContent = `
.mobile-top-sheet {
position: fixed;
top: 0;
left: 0;
right: 0;
z-index: 9999;
background: rgba(255, 255, 255, 0.88);
backdrop-filter: blur(12px);
-webkit-backdrop-filter: blur(12px);
box-shadow: 0 4px 24px rgba(0, 0, 0, 0.06);
padding: 10px 16px;
display: flex;
align-items: center;
justify-content: space-between;
gap: 12px;
transform: translateY(-110%);
opacity: 0;
transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.3s ease;
border-bottom: 1px solid rgba(241, 245, 249, 0.6);
}
.mobile-top-sheet.show {
transform: translateY(0);
opacity: 1;
}
.mbs-left {
display: flex;
align-items: center;
gap: 10px;
flex: 1;
min-width: 0;
}
.mbs-avatar-container {
width: 80px;
height: 45px;
flex-shrink: 0;
display: flex;
align-items: center;
justify-content: center;
overflow: hidden;
border-radius: 4px;
}
.mbs-avatar {
width: 100%;
height: 100%;
object-fit: cover;
}
.mbs-info {
display: flex;
flex-direction: column;
min-width: 0;
}
.mbs-title {
font-size: 0.88rem;
font-weight: 800;
color: #0f172a;
white-space: nowrap;
overflow: hidden;
text-overflow: ellipsis;
line-height: 1.25;
}
.mbs-subtitle {
font-size: 0.72rem;
font-weight: 500;
color: #64748b;
white-space: nowrap;
overflow: hidden;
text-overflow: ellipsis;
margin-top: 2px;
line-height: 1.2;
}
.mbs-action-btn {
padding: 6px 14px !important;
font-size: 0.78rem !important;
font-weight: 750 !important;
border-radius: 99px !important;
height: 32px !important;
display: flex !important;
align-items: center !important;
justify-content: center !important;
white-space: nowrap !important;
width: auto !important;
box-shadow: 0 4px 12px rgba(171, 14, 0, 0.2) !important;
}
`;
document.head.appendChild(style);
document.body.appendChild(sheet);
const actionBtn = sheet.querySelector('.mbs-action-btn');
if (actionBtn) {
actionBtn.addEventListener('click', () => {
if (typeof window.openRegModal === 'function') {
const pageCta = isEn ? "mobile_top_sheet_en" : "mobile_top_sheet_vi";
window.openRegModal(pageCta);
}
});
}
let lastScrollTop = 0;
window.addEventListener('scroll', () => {
const scrollY = window.scrollY;
if (scrollY > 100 && scrollY > lastScrollTop) {
sheet.classList.add('show');
} else if (scrollY < lastScrollTop || scrollY <= 100) {
sheet.classList.remove('show');
}
lastScrollTop = scrollY;
}, { passive: true });
}
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', () => {
initCopyrightYear();
initDegreeModal();
initVnGradModal();
initScrollSpy();
initWorkshopTabs();
initLanguageSwitcher();
initWordPressBlog();
initCardTiltGlow();
initCountersParallax();
initReadingProgressAndSideNav();
initMobileTopSheet();
});
} else {
initCopyrightYear();
initDegreeModal();
initVnGradModal();
initScrollSpy();
initWorkshopTabs();
initLanguageSwitcher();
initWordPressBlog();
initCardTiltGlow();
initCountersParallax();
initReadingProgressAndSideNav();
initMobileTopSheet();
}